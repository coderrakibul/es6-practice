//template string holo string jora deya
const firstName = "Rakibul";
const lastName = "Islam";

const fullName = ` ${firstName} ${lastName}`;
console.log(fullName);

const products = {
    name: "xiaomi",
    model: "note 10 pro",
    price: 27000,
    color: "vintage bronze"
};
const productDesc = `${products.name} ${products.price}`;
console.log(productDesc);